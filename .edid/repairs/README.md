# Repair packets

Failed required checks create versioned JSON repair packets here. Preserve failed packets as
history; later passing evaluations resolve them in state rather than deleting the evidence.
