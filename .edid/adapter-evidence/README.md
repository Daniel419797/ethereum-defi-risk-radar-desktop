# Local adapter evidence

Adapter runs write redacted JSON reports and logs here. Generated artifacts are ignored by
Git because provider output can contain operational details. Promote only a reviewed,
non-sensitive summary into `docs/verification.md`.
